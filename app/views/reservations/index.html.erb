<h1>My list of reservations</h1>
<%= link_to 'new', new_house_reservation_path(@house) %>

  <% @reservations.each do |reservation| %>
    <p>
      <%= reservation.house.name %>
    </p>
      <%= reservation.guests_number %>
      <%= reservation.start_booked_at %>
      <%= reservation.end_booked_at %>
  <% end %>




